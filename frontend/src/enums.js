export const CURRENCY_SYMBOL = 'Rs';
export const APP_PREFIX = 'Inventory Management System';

export const ValidationRegex = {
    email: /.+@.+/,
    passcode: /^(?=.*[a-z])(?=.*[A-Z])(?=.*[!@#$%^&*()_+{}\[\]:;<>,.?~\\-])/,
};

export default {
    APP_PREFIX,
    CURRENCY_SYMBOL,
    ValidationRegex,
};